<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.posco.standardmanagement.s20a01.domain.vehicle.mybatis.VehicleMapper">
    
    <!-- 예시 -->
    <!-- <select id="selectVehicleList" resultType="VehicleMybatisEntity">
        SELECT * FROM Vehicle_table
    </select>
    <select id="selectVehicle" resultType="VehicleMybatisEntity">
        SELECT * FROM Vehicle_table WHERE licensePlateNumber = #{id}
    </select> -->
</mapper>