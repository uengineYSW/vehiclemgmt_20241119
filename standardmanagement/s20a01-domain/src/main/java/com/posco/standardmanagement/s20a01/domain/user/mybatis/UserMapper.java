package com.posco.standardmanagement.s20a01.domain.user.mybatis;

import java.util.List;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface UserMapper {
    // 예시
    // List<UserMybatisEntity> selectUserList();
    // UserMybatisEntity selectUser(String id);
}
